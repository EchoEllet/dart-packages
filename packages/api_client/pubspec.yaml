name: api_client
description: "Enforces explicit failure handling for API calls."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:
  result:
    path: ../result
  json_utils:
    path: ../json_utils
  http: ^1.4.0
  http_method:
    path: ../http_method
  meta: ^1.16.0

dev_dependencies:
  test: ^1.24.0
  mocktail: ^1.0.4
