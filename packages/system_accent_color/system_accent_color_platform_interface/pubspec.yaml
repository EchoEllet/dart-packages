name: system_accent_color_platform_interface
description: A common platform interface for the system_accent_color plugin.
version: 0.0.1
repository: https://github.com/EchoEllet/dart-packages/tree/main/packages/system_accent_color_platform_interface
issue_tracker: https://github.com/EchoEllet/dart-packages/issues/new?labels=p%3A%20system_accent_color
topics: [system-accent-color, os-integration, dynamic-color, theming]

environment:
  sdk: ^3.10.0

dependencies:
  plugin_platform_interface: ^2.1.8
  # Required for dart:ui (Color)
  flutter:
    sdk: flutter
